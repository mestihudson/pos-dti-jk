{
  "name": "ws",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "live-server --watch=src/main.pdf --verbose --wait=1000 src",
    "compile": "nodemon --exec './compile.sh' -e 'tex'"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "nodemon": "^2.0.6"
  }
}
